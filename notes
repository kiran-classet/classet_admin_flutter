1. create a api service file so that every api call is to be travel into hat file. if any error or exception came from the api that will also be handled here.

2. base url of the app is : https://8bzo5ffosh.execute-api.ap-south-1.amazonaws.com/sasdev/v1/data/

3. In api call need to send the idtoken as bearer token.





4. After logging in to the app , call api and store data in riverpod. so that i can access through out the app.

5. api call 1 -> https://8bzo5ffosh.execute-api.ap-south-1.amazonaws.com/sasdev/v1/data/authorize/getAdminuserDetails/{cognito.getUserName()}
 get api 
6. In above api call send cognito username as path parameter.


7. now integrate shimmer effect for after login apis.
 
--------------------------------------------------------------------------------
----------------------------------------
----------------------------------------
----------------------------------------completed till above

b. bottom tab not to be moved. -- done
a. implement complete profile page based on login.

----------------------------------------
----------------------------------------


8. need an api to get admins assigned academic years for profile dropdown.
9. Need an api to get admins assigned boards and branches heirarchy. (send sections also based on if teacher loggs in.)
10. Pull down in home screen to call above apis.


--------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------

Need a single get api after login in mobile and pull to refresh at home screen.
1. get admin user info
2. assigned academic years
3. assigned boards , branches (If type teacher send assigned sections).
(Save all above data in the db after fetching the api for future cache......)

4. then build the bottom rb sheet based on heirarchy.

For approvals tab click calling apis and pull to refresh:
1. get approval types assigned to user dont show all approval types (Like if user dont have access for fee concession dont show this in dropdown).
2. Asssinged approval types and his assinged branches (at settings approval heirarchy) need to send at once.


check approval conditoins in backend :
0. No assinged persions can handle approvals
1. No, 2 same level approvals happen (then refresh show error message)
2. If senior appoval done then reject the low level (then refresh show error message)
3. 


---------------------------------------

1. change ui for filters. test filters. --done
2. dynamic icon generation for app.
3. firebase integration for push notifications.
4. integrate communication module.






